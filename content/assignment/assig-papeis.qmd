---
title: "Fator, bloco ou covariável?"
subtitle: "Atividade extraclasse 04"
author:
  - "Prof. Fabio Cop (*fcferreira@unifesp.br*)"
  - "Instituto do Mar - Unifesp"
date: today
lang: pt-BR
language:
  title-block-author-single: ""
  title-block-author-plural: ""
format:
  html:
    toc: true
    toc-title: "Conteúdo"
    toc-depth: 2
    code-tools: true
---

# Três papéis, uma mesma fórmula

Fator, bloco e covariável entram todos com `+` ou `*` na fórmula do modelo, e o R os ajusta pelo mesmo procedimento. O que os separa não está na fórmula: o fator é o que se quer testar, enquanto bloco e covariável são retirados do resíduo para tornar o teste mais sensível.

Esta atividade pede que você reconheça esse papel em situações novas, e que mostre no R o que acontece quando ele é ignorado.

```{r}
library(tidyverse)
theme_set(theme_bw(base_size = 12))
```

# Quatro situações

Cada situação abaixo descreve um estudo. Para cada uma, decida o papel de cada variável mencionada e justifique.

1. Um experimento testa três rações para camarões em 12 tanques, montados em três salas com temperaturas ligeiramente diferentes. **O que é a sala?**
2. Um estudo compara o crescimento de mudas de mangue em dois substratos. A altura inicial de cada muda foi medida antes do plantio. **O que é a altura inicial?**
3. Um experimento cruza duas temperaturas e três salinidades para avaliar a sobrevivência de larvas. **O que são temperatura e salinidade?**
4. Um levantamento compara a riqueza de peixes entre dois estuários. A profundidade de cada ponto de coleta foi registrada. **O que é a profundidade?**

# Uma simulação para a situação 1

A simulação a seguir reproduz a estrutura da situação 1: três rações, testadas em tanques distribuídos por salas com temperaturas diferentes.

```{r}
experimento <- tibble(
  sala = rep(1:6, each = 3),
  racao = rep(c("A", "B", "C"), times = 6)
) |>
  mutate(
    media_esperada = 20 + 4 * sala,
    resposta = media_esperada + rnorm(18, mean = 0, sd = 1.2)
  )

sem_sala <- aov(resposta ~ racao, data = experimento)
com_sala <- aov(resposta ~ racao + factor(sala), data = experimento)

summary(sem_sala)
summary(com_sala)
```

::: {.callout-tip appearance="minimal" title="Para pensar"}
Nesta simulação `racao` não tem efeito nenhum (a média não depende de `racao`). Ainda assim, compare o quadrado médio residual dos dois modelos: tratar a sala como se ela não existisse muda a régua usada para julgar a ração?
:::

# Entrega

::: {.callout-important appearance="minimal" title="Entregue"}
Um documento curto ou um script `ae-papeis.R` com comentários, contendo:

1. para cada uma das quatro situações: o papel de cada variável (fator, bloco ou covariável), a justificativa em uma frase, e a fórmula do modelo em R que você usaria;
2. uma simulação, adaptada da simulação acima, mostrando o efeito de tratar uma variável de bloco como se ela não existisse (use a situação 1 como base);
3. em comentários, duas a três frases respondendo: **o que distingue um bloco de um fator, se os dois aparecem do mesmo jeito na fórmula do modelo?**
:::

::: {.callout-tip appearance="minimal" title="Leitura adicional"}
- [Associação entre variáveis quantitativas e qualitativas](https://fcopf.github.io/mead/content/medidas-associacao/biquantquali.html){target="_blank"} (MEAD)
:::
